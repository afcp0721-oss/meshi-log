# メール認証の導入手順（本番反映前に実施）

実装だけでは有効になりません。まず staging で確認し、ユーザーのレビュー後に本番へ反映します。既存 Worker を先に置き換えると未登録利用者は利用できなくなります。

1. Firebase `meshi-log-auth` に Web アプリを登録。Authentication のメール/パスワードを有効化。メールリンク認証・電話認証は不要です。
2. 許可ドメインに `afcp0721-oss.github.io` と staging Worker のホストを登録。確認・再設定メールを日本語で設定します。メール列挙保護を有効にし、サーバー側パスワードポリシーも12文字以上にします。Firebase側のメール送信制限も確認してください。ブラウザーの60秒制限だけでは濫用対策になりません。
3. Worker の `FIREBASE_PROJECT_ID` にプロジェクトID、`FIREBASE_WEB_CONFIG` に Web 設定の JSON（apiKey, authDomain, projectId, appId）を登録。Firebase Web apiKey は公開設定です。Geminiの秘密鍵とは別です。サービスアカウント秘密鍵は不要です。
4. 利用者が設定からメール登録→確認メールのリンクを開く。運営者がFirebaseコンソールで確認済みアカウントのUIDを確認します。
5. Worker Secret `EMAIL_USERS` に UID と記録IDを対応付けます。例：`{"firebase-uid":{"userId":"verified-record-id","expiresAt":"2027-01-01T00:00:00Z","validAfter":0}}`。新規利用者には一意のランダムIDを割り当てます。既存利用者は元端末と本人を確認して旧記録IDを割り当て、他人から申告されたIDだけでは移行しません。メールアドレスは記録DBには保存しません。
6. 対象D1に `migrations/0001_ai_usage.sql` を適用。staging と本番を取り違えないこと。activity_records や画像保管期間は変更しません。
7. `GLOBAL_DAILY_AI_UNITS` を設定。例150は、10人×5回×最大3枚の上限に相当します。写真1枚の生成を1単位、過去ログAI補助を1単位と数えます。金額そのものの上限ではありません。未設定ならAI生成は停止します。ACCESS_LIMITER / AI_LIMITER はwrangler設定の通り必要です。
8. stagingで実メール登録・確認・ログイン・再設定、別端末から同じ履歴、未承認/別人の拒否、5回→6回の拒否、Discordなし保存とDiscordあり保存を確認。確認メール/再設定メールは本人操作で送信します。
9. 本番利用者の対応付けと設定を準備し、レビュー承認後に本番反映。Firebase設定や実メールの確認が終わるまで完了扱いにしません。

## 回数と費用

1人1日5回、日本時間0時リセット。3枚一括は利用者の1回ですが全体枠は3単位です。確認生成後の保存は追加回数なし。再生成は1回。AI開始後の失敗も消費します。保存・編集・過去ログ閲覧は対象外です。全体枠と個人枠を同じD1の原子的な書込みで予約します。Geminiの出力は1呼び出し2048トークンまで。請求額はモデル/入力/画像/再試行等で変わるため、これは円建ての請求停止機能ではありません。Gemini側の割当と請求通知も別途設定してください。

## 移行・停止・復旧

同じFirebase UIDは同じ記録IDに対応します。機種変更後も同じメールでログインすれば履歴を参照できます。Discord URL・相棒の名前等の端末設定は同期されません。メールを失った場合は運営者が別経路で本人確認し、新しいUIDへ対応付けます。古い対応は削除します。

緊急停止はEMAIL_USERSの該当エントリ削除/disabled:true。Firebase側の無効化だけでは発行済みトークンが期限まで通る場合があります。validAfterは認証時刻の下限（UNIX秒）です。旧認証なしWorkerへのロールバックは情報を再公開するため避けてください。利用不可にして修正版を反映します。
